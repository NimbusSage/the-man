NODE_ENV=development
PORT=3000
HOST=0.0.0.0

DATABASE_URL=postgresql://theman:theman_dev_password@localhost:5432/theman
REDIS_URL=redis://localhost:6379

# Generate a real secret before deploying anywhere real users can reach:
#   openssl rand -base64 48
JWT_SECRET=change-me-to-a-random-secret-at-least-32-characters-long

CORS_ORIGINS=http://localhost:5173
